{{ include "my-pack.fullname" . }} has been deployed (wrapping Podinfo).

{{- if .Values.nebariapp.enabled }}

NebariApp is ENABLED. The nebari-operator will configure:
  - HTTPRoute for hostname: {{ .Values.nebariapp.hostname }}
  {{- if .Values.nebariapp.auth.enabled }}
  - OIDC authentication via {{ .Values.nebariapp.auth.provider }}
  {{- end }}

Check the NebariApp status:
  kubectl get nebariapp {{ include "my-pack.fullname" . }} -n {{ .Release.Namespace }}

{{- else }}

NebariApp is DISABLED. To access Podinfo locally:

  kubectl port-forward svc/{{ .Release.Name }}-podinfo 9898:9898 -n {{ .Release.Namespace }}

Then open http://localhost:9898 in your browser.

{{- end }}
